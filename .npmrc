# pnpm configuration for YoungHouseWeb
# Enable global store to share packages across projects (saves disk space)
store-dir=~/.pnpm-store

# Hoist node_modules to be compatible with Next.js
hoist=true
shamefully-hoist=true

# Auto-install peers
auto-install-peers=true

# Skip optional dependencies if they fail
optional=true
